# Neon Postgres connection string
DATABASE_URL=postgres://user:password@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require

# Server
PORT=3000

# CORS — restrict to your Expo tunnel/production URL in prod
CORS_ORIGIN=*

# Set to "true" to skip JWT verification during local dev
BYPASS_AUTH=true
